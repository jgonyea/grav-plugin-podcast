{% extends 'partials/base.html.twig' %}

{% block content %}
    <h1>{{ header.title }}</h1>
    {{ page.content }}
    <div class="podcast-meta">
        <h2>{{ header.date|truncate(10, true) }}</h2>
        <h2>{{ header.taxonomy.speaker[0] }}</h2>

    </div>

    {{ page.media[ (page.header.podcast|first|first).name ].html() }}


{% endblock %}